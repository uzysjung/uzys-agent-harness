<!--
GitHub Issue body 템플릿 (gh-issue-workflow skill) — 변형 2종: Task · Epic

- 5섹션 전부 채울 필요는 없다. 비어 있으면 그 섹션을 통째로 지운다 (placeholder 금지).
- BDD 매핑: 전제(Given) → 적용 대상(When) → AC(Then).
- 방향성 상태로 착수 가능 여부가 갈린다 (OPEN = 차단, "YYYY-MM-DD 확정" = 착수 가능).
- 등록 전 준비도 8줄을 센다 — references/readiness.md §4.
- 라벨·마일스톤·부모는 본문이 아니라 gh 플래그로 붙인다 — references/hierarchy.md §2.
-->

# 변형 A — Task (하위 이슈 / 낱개 작업)

## 배경

[왜 이 작업이 필요한가. 1-3 문장. 관측된 증상, 도달하려는 상태, 맥락.
 이 이슈만 읽고 착수 가능해야 한다 — 링크 뒤에 있는 요지는 여기 옮겨 적는다.]

## 전제 (Given)

[시작 전에 충족돼야 하는 조건. 다른 이슈 / 외부 의존 / 결정 / 인프라 상태. 미충족이면 착수 차단.]

- [ ] [예: #N 완료]
- [ ] [예: 스키마 v3 마이그레이션 적용]

## 방향성 (OPEN | YYYY-MM-DD 확정)

[OPEN = 사용자 결정 대기. 확정이면 날짜와 채택 근거를 남긴다.]

- 옵션 A: [설명]
- 옵션 B: [설명]
- **채택 (확정 시)**: [고른 것 + 왜 다른 것을 안 골랐는지 한 줄]

방향성이 OPEN 이면 이 이슈로 작업을 시작하지 않는다.

## 적용 대상 / Acceptance Criteria (When → Then)

[건드릴 파일·모듈·심볼을 먼저 적고, 그 다음 pass/fail 로 갈리는 완료 조건.]

대상: `[path/to/file.ts]` 의 `[symbol]` · `[path/to/other]`

- [ ] [When <조건> → Then <관측 가능한 결과>]
- [ ] [경계 하나 — 예외·edge 경로]
- [ ] [위를 고정하는 회귀 테스트가 변경 없이는 red]

## 후속 작업 (Next)

- [ ] [#M 으로 분리한 것 — 이번 범위에서 뺀 것과 그 이유]

---

# 변형 B — Epic (부모 이슈 / 묶음)

<!--
에픽은 직접 구현되지 않는다. AC 대신 "하위와 순서"를 담는다.
하위가 3개 미만이면 에픽을 만들지 않는다 — 껍데기만 는다.
-->

## 배경

[이 묶음이 왜 필요한가. 무엇이 지금 안 되는가. 2-4 문장.]

## 방향성 (OPEN | YYYY-MM-DD 확정)

[하위 전체가 공유하는 결정. 하위마다 다시 정할 필요가 없어야 에픽이 값을 한다.]

- **채택**: [방침 + 근거]
- **하지 않기로 한 것**: [빼기로 한 것 + 왜] ← 이걸 적어야 다음 사이클에 다시 논의되지 않는다

## 하위 작업과 순서

[gh 로 부모-자식을 붙이면 GitHub 이 진척을 자동 표시한다. 여기에는 **순서와 이유**를 적는다.]

1. #N — [무엇] · 먼저 하는 이유: [의존/위험]
2. #M — [무엇] · #N 의 결과가 있어야 판정 가능
3. #L — [무엇] · 독립. 언제든

## 완료 판정 (에픽이 닫히는 조건)

[하위가 다 닫혔다는 사실만으로 닫지 않는다. 묶음 수준에서 관측되는 것을 적는다.]

- [ ] [When <사용자 표면 조건> → Then <관측 결과>]

## 범위 밖 (Non-Goals)

- [이 에픽에서 다루지 않는 것 — 나중에 "왜 저건 안 했지"를 막는다]
