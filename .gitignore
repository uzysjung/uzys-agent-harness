.dev-references/
!.dev-references/cherrypicks.lock
.DS_Store
.agents/
.goose/
skills-lock.json
.claude/CLAUDE.md.backup
.claude/settings.local.json
.claude/settings.local.json.backup
.claude/gate-status.json

# --update 플래그가 생성하는 백업 디렉토리
.claude.backup-*/

# 시크릿 환경 변수
.env
.env.local
.env.*.local

# Session-specific artifacts
.claude/scheduled_tasks.lock
# compaction-handoff transient resume anchor (snapshot, overwritten each handoff — ADR-026)
.handoff/
Docs/checkpoints/
docs/checkpoints/
.claude/codebase-map.json

# 하네스 런타임 산출물 — 설치 로그 + 훅 차단 로그(hook-blocks.log).
# 이 리포도 자기 하네스를 쓰므로 차단이 일어나면 여기에 쌓인다.
.uzys-agent-harness/

# npx skills 다운로드 캐시 (자동 생성, git 추적 제외)
.factory/
.claude/local-plugins/

# HITO raw logs (private usage data; aggregated in docs/evals/hito-baseline-*.md)
.claude/evals/hito-*.log

# CLI rewrite (Phase A+, npm/Node)
node_modules/
dist/
coverage/
*.tsbuildinfo
.vitest-cache/
.claude/skills/
