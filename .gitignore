.dev-references/
!.dev-references/cherrypicks.lock
.DS_Store
.agents/
.goose/
skills-lock.json
.claude/CLAUDE.md.backup
.claude/settings.local.json
.claude/settings.local.json.backup
.claude/gate-status.json

# --update 플래그가 생성하는 백업 디렉토리
.claude.backup-*/

# 시크릿 환경 변수
.env
.env.local
.env.*.local

# Session-specific artifacts (P0-3 checkpoint-snapshot)
.claude/compact-warning.flag
Docs/checkpoints/
docs/checkpoints/
.claude/codebase-map.json

# npx skills 다운로드 캐시 (자동 생성, git 추적 제외)
.factory/
.claude/local-plugins/

# HITO raw logs (private usage data; aggregated in docs/evals/hito-baseline-*.md)
.claude/evals/hito-*.log

# CLI rewrite (Phase A+, npm/Node)
node_modules/
# dist/ 는 git 에 커밋 (v26.72.0) — npx github: 가 prepare 빌드 없이 즉시 실행.
#   npm 10 GitFetcher 버그(prepare 있는 git-dep pack 실패) 회피 + 모든 npm 버전 작동.
#   freshness 는 test.yml 의 dist-freshness 스텝이 강제 (build → git diff).
coverage/
*.tsbuildinfo
.vitest-cache/
.claude/skills/
