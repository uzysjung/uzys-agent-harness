---
status: active
---

# 문서 체계 재정비 (F 사이클)

> 이 파일은 **순서와 레인**만 소유한다. 완료 기준은 `docs/SPEC.md` 의 AC 다 — 여기서 재서술하지
> 않고 AC 번호로 참조한다. 설계 = `doc-template-standard.md`, 독립 검토 = `doc-template-standard-review.md`.

## 순서

- [x] **옛 SPEC 앵커를 `docs/archive/` 로 이동 + 현행 SPEC 작성** — 설계 / 사용자 승인 2026-07-29
- [ ] **의무 계수기 착지** (AC1) — 구현. 착지 전까지 설계의 의무 *내역*(무조건/조건부/메타·가중)은 미확정이다
- [ ] **G-F1 · G-F2 · G-F4 신설 후 red 를 눈으로 본다** (AC3·AC4·AC6) — 테스트 레인. **정합 작업 전에** 돌린다
- [ ] **`docs/templates/` 4종 생성 + `change-management` 인라인 골격 축소 + G-F3 · G-F6 신설** (AC2·AC5) — 구현
- [ ] **ADR Status 표기·값 정규화** (AC3) — 구현. G-F2 위반 29건 → 0
- [ ] **ADR 관계 표기 대칭화** (AC6) — 구현 + 사용자 결정(전면/부분이 갈리는 건)
- [ ] **plans frontmatter 부여** (AC4) — 구현. 겸해서 `harness-direction-2026-07-27-todo.md:144` 의 `shipped(<태그>)` 를 `shipped` 로 정정
- [ ] **게이트 6종 음성 대조 양방향** (AC7) — 검증 레인. 코드를 쓰지 않은 레인이 직접 다시 돌린다
- [ ] **독립 리뷰** — 이 사이클 산출물을 쓰지 않은 레인

## 막힌 것

- **`.git/hooks` 기본 설치 여부** — A 사이클 선행 결정. 사용자 판단 대기. 이 사이클을 막지는 않는다.
- **앵커 절 구성 변경**(배포물 `src/project-claude-merge.ts`, 현행 6절 → 목표 4절) — 선행 계획이
  착지해야 기준이 선다. 안 서면 이번 사이클에서 뺀다(설계 §6-1 [검토정정 H5]).

## 미확인

- **Claude Code 가 룰 frontmatter 의 `paths:` 를 존중하는가** — 존중하면 이 리포 상주가 126→110
  이 될 수 있다(설계 §10 [검토정정 H2]). 실행 관측이 없어 미확인이고, 그래서 R1 은 기각 상태다.
  풀리는 조건: `paths:` 를 단 룰이 매칭 밖 세션에서 실제로 로드되지 않는지 한 세션 관측.
- **의무 임계값의 근거 논문 4편** — 독립 검토 레인이 원문을 열지 않았다(리포 안에 사본이 없다).
  임계 11.9 가 템플릿 축소의 직접 근거라 여기가 틀리면 설계 §3 이 흔들린다.
  풀리는 조건: 4편의 해당 수치를 원문에서 대조.
