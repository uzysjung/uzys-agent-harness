# 검증 보고 — #293 ack 누출 (독립 레인)

- 대상 커밋: `1c91ff6` (브랜치 `fix/ack-scope-narrowing`, main `ea19b58` 위 커밋 1개)
- 검증 레인: 구현에 관여하지 않은 별도 에이전트. 구현 보고를 읽는 대신 **명령을 직접 실행**해 증거를 만들었다.
- 실행일: 2026-08-10 · 저장소 상태: 사용자 소유 파일 2건(`.claude/settings.json`·`.claude/skills/.DS_Store`)만
  수정된 채로 시작해 **끝날 때까지 그대로**다(git 상태 변경 명령 0회, 전 변이 `cp` 스냅샷 → 역치환 복원 → `shasum -c` 전수 OK).

## 판정

**조건부 머지** — 주장한 것은 전부 재현됐고 과잉 차단은 0건이지만, 이 PR 이 넣은 회귀 테스트가
**게이트의 실제 판정 경로를 잠그지 않아** 호출부를 옛 방식으로 되돌리는 변이가 `vitest`·`biome`·`tsc`
전부에서 살아남는다(T0). 게이트를 고치는 PR 이 자기 수정을 게이트로 지키지 못한다.

| Severity | 건수 | 상태 |
|---|---|---|
| CRITICAL | 0 | pass |
| HIGH | 2 | warn — 수정 권장 |
| MEDIUM | 2 | info |
| LOW | 3 | 참고 |

---

## A. 재현 — 수정 전 판에서 M10 변이가 정말 살아남았는가

```
git show 1c91ff6^:tests/resident-doc-asset-reachability.test.ts > $S/gate.pre.ts   # sha 0266b3a6…
cp templates/CLAUDE.md $S/CLAUDE.md.orig
printf "\n- Hand implementation to the \`implementer\` agent.\n" >> templates/CLAUDE.md
cp $S/gate.pre.ts tests/__pre293.test.ts
npx vitest run tests/__pre293.test.ts   →  EXIT_PRE_M10=0   (3 tests passed)
```

**관찰**: EXIT 0. 변이 생존. 앵커는 159줄이 되고 새 줄의 `implementer` 지목이 통과했다.
**판정**: 구현 레인 주장 ① **재현됨**.

부수 실측 — 커밋 메시지가 "121자 앞"이라 쓴 거리를 직접 쟀다:
`` `implementer` `` 매치 시작 = offset 7622, 직전 `where installed` 시작 = 7485.
**시작→시작 137자**, ack 끝→매치 시작 122자. **121 은 어느 기준으로도 나오지 않는다**(LOW-1).

## B. 수정 후 같은 변이가 무는가 — 줄·자산·미설치 트랙 정확성

```
npx vitest run tests/resident-doc-asset-reachability.test.ts   →  EXIT_POST_M10=1   (1 failed | 3 passed)
  CLAUDE.md:159  →  implementer (미설치 트랙: executive, growth-marketing, project-management)
```

**판정**: 주장 ② **재현됨**. 줄 번호(159) · 자산(`implementer`) · 미설치 트랙 3종 전부 정확.
복원 후 `templates/CLAUDE.md` shasum `b4031c1b…` = baseline 일치.

## C. 과잉 차단 검사 — 지목 전수 대조 (이 보고서의 핵심)

방법: 게이트 원본에 probe `describe` 를 **덧붙인** 임시 파일(`tests/__probe293.test.ts`)로
`buildIndexes()`·`referenceRegex()`·`ackScope()`·`ABSENCE_ACK` 를 그대로 호출해 **모든 매치**를 덤프했다.
구현을 베껴 쓰지 않고 실제 함수를 부른다. 옛 범위는 diff 에서 삭제된 표현
`text.slice(Math.max(0, at - ACK_WINDOW), at + len + ACK_WINDOW)` 를 그대로 썼다.

상주 문서 8종(앵커 + 룰 7)에서 지목은 **총 5건**(게이트의 `references` canary 실측 5와 일치).

| # | 문서:줄 | 자산 | 미설치 트랙 수 | 수정 전 | 수정 후 | 면제 근거 위치 | 변화 |
|---|---|---|---|---|---|---|---|
| 1 | `CLAUDE.md:149` | `clear-korean-communication` | **3** (executive·growth-marketing·project-management) | 면제 `where installed` | 면제 `where installed` | 같은 줄, 매치 +30자 | 없음 |
| 2 | `CLAUDE.md:152` | `task-brief` | **0** | 면제 `where installed` | 면제 `where installed` | 같은 줄, 매치 +14자 | 없음(면제 불요) |
| 3 | `CLAUDE.md:156` | `model-orchestration` | **11 (전 트랙)** | 면제 `where installed` | 면제 `where installed` | 같은 줄, 매치 +23자 | 없음 |
| 4 | `rules/playwright-launch.md:3` | `ui-visual-review` | **0** | ack 없음 | ack 없음 | — | 없음(면제 불요) |
| 5 | `rules/playwright-launch.md:14` | `ui-visual-review` | **0** | ack 없음 | ack 없음 | — | 없음(면제 불요) |

범위 축소 실측(문자): 348→190 · 332→174 · 290→131 · 326→218 · 295→139. **모두 좁아졌고 판정은 전부 동일.**

**판정**: 현재 트리에서 **"전에는 면제, 지금은 위반"은 0건**. 과잉 차단 없음.
주장 ③(정당 면제 2건 보존) · ④(실제 면제 2건뿐 · 둘 다 같은 줄 · +30/+23자 · playwright-launch 는 미설치 0종)
**전부 실측으로 확인**. `task-brief` 도 ack 를 갖지만 미설치 0이라 "실제 면제"에는 안 든다 — 주장과 정합.

> 단서: 모집단이 5건뿐이다. 이 대조가 "안전하다"고 말할 수 있는 범위는 **오늘의 8개 문서**까지다.
> 미래 형태에 대한 위험은 D 항목이 답한다.

### 단조성 증명 — "기존보다 넓어지는 경우가 없다"

수식상 `max(blockStart, at-160) ≥ max(0, at-160)` 이고 `min(blockEnd, at+len+160) ≤ at+len+160` 이라
새 범위 ⊆ 옛 범위다. 말로 끝내지 않고 **fuzz 3,000 케이스**(표·인용문·코드펜스·헤딩·탭 항목·번호 항목·
`---`·EOF 개행 유무를 무작위 조합)로 확인했다:

```
FUZZ cases=3000 widened=0 newExemptButOldNot=0     EXIT=0
```

**판정**: 커밋 메시지의 "기존보다 넓어지는 경우가 없다" **검증됨**. 예외 0건, 크래시 0건.

## D. 누출 사냥 — 22종 실행

전부 실제 파일을 변이해 **진짜 게이트**를 돌렸다(합성 단위 호출이 아니라 end-to-end).
`PRE` 는 `1c91ff6^` 판, `POST` 는 커밋 판. 전 항목 복원 shasum 일치.

| 변이 | 대상 | PRE | POST | 결과 | 판정 |
|---|---|---|---|---|---|
| M10 원 재현(빈 줄 + 불릿) | CLAUDE.md | 0 | **1** | 사살 | 의도대로 |
| **M10b lazy continuation(불릿 없이 한 줄)** | CLAUDE.md | 0 | **0** | **생존** | **HIGH-2** |
| M10c 불릿, 빈 줄 없이 | CLAUDE.md | 0 | 1 | 사살 | 의도대로 |
| M10d 중첩 불릿(`  - `) | CLAUDE.md | 0 | 1 | 사살 | 의도대로 |
| M10e 들여쓴 이어짐(`  Hand …`) | CLAUDE.md | 0 | 0 | 생존 | 같은 항목 — 설계상 허용 |
| **D1 표 — 한 행의 조건절을 다른 행이 차용** | doc-governance | 0 | **0** | **생존** | **MEDIUM-1** |
| D2 중첩 리스트: 자식이 부모 조건절 차용 | doc-governance | 0 | 1 | 사살 | 의도대로 |
| **D3 중첩 리스트: 부모 지목 + 자식 불릿의 조건절** | doc-governance | 0 | **1** | 새로 차단 | **MEDIUM-2**(과잉 차단 후보) |
| **D4 인용문(`> 없으면`)이 옆 문단 지목을 면제** | doc-governance | 0 | **0** | **생존** | **MEDIUM-1** |
| **D5b 코드펜스 안 `없으면` 이 면제로 쓰임** | doc-governance | 0 | **0** | **생존** | **MEDIUM-1** |
| D15 항목 안 들여쓴 코드펜스가 면제로 쓰임 | doc-governance | 0 | 0 | 생존 | 같은 항목 — 경미 |
| D6 EOF 개행 없음 + 같은 줄 ack | doc-governance | — | 0 | 통과 | 과잉 차단 없음 ✓ |
| D7 EOF 개행 없음 + 이웃 항목 ack | doc-governance | 0 | 1 | 사살 | 의도대로 ✓ |
| D8 offset 0(첫 줄) + 같은 줄 ack | doc-governance | — | 0 | 통과 | 크래시 없음 ✓ |
| D9 offset 0 + 다음 줄 새 항목 ack | doc-governance | 0 | 1 | 사살 | 의도대로 ✓ |
| **D10 문단 중간 `- 5도 …` 줄이 블록을 자름** | doc-governance | 0 | **1** | 새로 차단 | **LOW-2** |
| D11 리스트 직후 비들여쓰기 문단(lazy) | doc-governance | 0 | 0 | 생존 | M10b 와 동종 |
| **D12 같은 문단 다른 줄의 조건절** | doc-governance | 0 | **0** | **생존** | 블록 단위의 구조적 한계 |
| **M16 한 항목 안 두 자산 — 한쪽 조건절이 다른 쪽 면제** | doc-governance | 0 | **0** | **생존** | **HIGH-2 와 동종** |
| D13 창 경계 정확히 −160자 | doc-governance | — | 0 | 통과 | off-by-one 없음 ✓ |
| D14 창 경계 −161자 | doc-governance | 1 | 1 | 사살 | 경계 정확 ✓ |
| D16 리스트 앞 도입 문장의 조건절 | doc-governance | 0 | **1** | 새로 차단 | LOW-3(의도된 좁힘) |
| D17 직전 문단의 조건절(빈 줄로 분리) | doc-governance | 0 | **1** | 새로 차단 | LOW-3(의도된 좁힘) |

### 게이트 구현 변이 — 새 테스트의 3개 단언이 각각 무는가

| 변이 | 무엇을 되돌렸나 | EXIT | 무는 단언 | 복원 sha |
|---|---|---|---|---|
| **T0** | `findViolations` **호출부**만 옛 창 방식으로(`ackScope` 정의는 남김) | **0** | **없음 — 생존** | eee0f437… OK |
| T1 | `ackScope` 본체를 옛 창으로 | 1 | `이웃 항목의 조건절을 빌려 쓰지 못한다` | eee0f437… OK |
| T2 | 전방 확장(이어지는 줄) 제거 | 1 | `같은 항목의 이어지는 줄은 자기 블록이다` | eee0f437… OK |
| T3 | 범위를 빈 문자열로 | 1 | `자기 블록의 조건절은 면제로 인정한다` **+ 본 게이트 2건 위반 노출** | eee0f437… OK |
| **T4** | `BLOCK_START` 의 `^\s*` → `^` (들여쓴 불릿을 항목으로 안 봄) | **0** | **없음 — 생존** | eee0f437… OK |

T0 에 대해 다른 게이트도 확인했다(변이 상태에서 직접 실행):
`npx biome check` **EXIT 0** · `npx tsc --noEmit` **EXIT 0**. **CI 전체가 초록이다.**

**판정**: 주장 ⑤(옛 창으로 되돌리면 red) **재현됨 — 단, `ackScope` 를 되돌릴 때만**이다.
호출부를 되돌리면 아무도 안 문다.

## E. 새 회귀 테스트가 구현을 베낀 것인가

- **합성 입력의 대표성 — 부분적으로 있다.** `- \`alpha\`, where installed — …` + 2칸 들여쓴 이어짐은
  실제 앵커 149~157줄의 형태를 그대로 옮긴 것이다(C 표 1·3행과 같은 모양). 통과하도록 꾸민 입력은 아니다.
- **다만 계약이 아니라 헬퍼를 고정한다.** 세 단언 모두 `ackScope()` 를 직접 부른다. 게이트의 판정
  함수 `findViolations` 를 거치지 않으므로 **"게이트가 좁은 범위를 쓴다"는 계약은 어디에도 고정돼 있지 않다**
  (T0 가 그 증거다). #290 사이클이 같은 파일에 넣은 canary 는 반대였다 — `buildIndexes()` 결과를
  직접 단언해 모집단 축소를 물게 했다. 같은 강도의 결속이 이번엔 없다.
- **덮지 않은 형태**: 표 · 인용문 · 코드펜스 · 문단 · EOF · offset 0 · 창 경계. 내가 D 에서 돌려 보니
  그중 셋에서 누출이, 둘에서 새 차단이 나왔다. 즉 미시험 영역이 곧 결함 영역이었다.

**판정**: 자기만족 테스트는 아니다(T1·T2·T3 가 각각 다른 단언을 사살). 그러나 **결속 지점이 틀렸다**.

## F. `npm run ci` 직접 실행

```
npm run ci > $S/F.ci.log 2>&1
echo "NPM_RUN_CI_EXIT=$?"     →  NPM_RUN_CI_EXIT=0     (파이프 없이 읽음)
```

| 항목 | 실측 | 구현 레인 주장 | 일치 |
|---|---|---|---|
| exit code | 0 | 0 | ✓ |
| Tests | **1339 passed** (Test Files 92 passed) | 1339 | ✓ |
| branches | **88.8%** (임계 88) | 88.8% | ✓ |
| lines / statements / functions | 96.9 / 96.4 / 96.11 | — | 임계 90 통과 |
| biome | `Checked 138 files. No fixes applied.` | — | ✓ |

부수: `bash templates/scripts/spec-drift-check.sh ship` **EXIT 0** (`OK: SPEC/TODO 동기화 상태 정상`).

## G. 범위 침범과 주석 사실성

- **변경 범위**: `git show 1c91ff6 --stat` = **1 파일**, `tests/resident-doc-asset-reachability.test.ts`
  (+77/−6). 템플릿·소스·문서 무변경. **#293 밖 침범 0건.**
- **DO NOT CHANGE / Non-Goals**: 해당 없음(테스트 1파일). `ACK_WINDOW=160` 과 `ABSENCE_ACK` 는 그대로 두었다 —
  ADR-055 가 정한 어휘 목록을 건드리지 않은 것은 옳은 선택이다.
- **로직 사본 여부**: `ACK_WINDOW|ABSENCE_ACK|ackScope` 전수 grep → 코드 사본은 이 파일 하나뿐
  (나머지 2건은 `docs/plans/` 산문). **surface parity 결함 없음.**

세 주장 개별 판정:

| 주장 | 실측 | 판정 |
|---|---|---|
| "실제 ACK 면제는 2건뿐" | 미설치>0 이면서 ack 로 살아난 지목 = `CLAUDE.md:149`·`:156` 정확히 2건 | **참** |
| "둘 다 ack 가 지목과 같은 줄(+30자·+23자)" | +30 / +23, 줄바꿈 미포함 확인 | **참** |
| "기존보다 넓어지는 경우가 없다" | 수식 증명 + fuzz 3,000 케이스 widened=0 | **참** |
| (부수) "121자 앞의 다른 항목" | 시작→시작 **137자**, ack 끝→매치 시작 **122자** | **불일치 — LOW-1** |
| (부수) "`playwright-launch` 지목 2건은 미설치 0종" | 둘 다 missing=0 | **참** |

---

## 결함

### [HIGH-1] 회귀 테스트가 게이트 판정 경로를 잠그지 않는다 — 호출부 변이 생존

- **파일**: `tests/resident-doc-asset-reachability.test.ts:258`(호출부) / `:302-319`(새 테스트)
- **증거**: `findViolations` 안의 `if (ABSENCE_ACK.test(ackScope(text, at, m[0].length))) continue;` 를
  삭제된 옛 두 줄로 되돌린 변이(T0)에서 **vitest EXIT 0 · biome EXIT 0 · tsc EXIT 0**.
  `ackScope` 는 정의만 남아 미사용이 되지만 아무도 문제 삼지 않는다.
- **왜 문제인가**: 이 PR 의 산출물 자체가 게이트다. 그 게이트의 유일한 새 계약이 "면제 범위가 좁다"인데,
  그것을 다시 넓혀도 CI 가 초록이다. 이 리포의 성문 기준 — *변이가 살아남으면 게이트는 장식이다* — 정면 위반.
  #290 사이클은 같은 파일에서 `buildIndexes()` 를 직접 단언해 이 결속을 확보했다. 전례가 있는데 이번엔 빠졌다.
- **재현**:
  ```bash
  # 호출부 두 줄을 옛 판으로 되돌린 뒤
  npx vitest run tests/resident-doc-asset-reachability.test.ts ; echo "EXIT=$?"   # → 0
  ```
- **수정 방향은 적지 않는다**(구현 레인 몫). 판정 기준만 남긴다: 호출부를 되돌린 변이가 **red** 여야 한다.

### [HIGH-2] 이슈 #293 이 지목한 구역에서 "줄 추가" 누출이 형태만 바꾸면 여전히 통과

- **증거**: 앵커 끝에 **불릿 없이** 한 줄을 덧붙이면(`M10b`) `model-orchestration` 항목의 `where installed`
  가 새 `implementer` 지목을 여전히 덮는다 → **EXIT 0**. 같은 클래스가 4건 더 생존:
  `M16`(한 항목 안 두 자산) · `D12`(한 문단 안 다른 줄) · `D11`(리스트 직후 비들여쓰기 문단) · `M10e`.
- **재현**:
  ```bash
  printf "Hand implementation to the \`implementer\` agent.\n" >> templates/CLAUDE.md   # 앞 개행·불릿 없음
  npx vitest run tests/resident-doc-asset-reachability.test.ts ; echo "EXIT=$?"          # → 0
  ```
- **판정의 결**: 커밋 메시지는 "이웃 **항목**의 조건절"이라 범위를 정직하게 좁혀 적었으므로 **메시지는 거짓이 아니다**.
  그러나 이슈 #293 의 제목은 "한 자산의 조건절이 **이웃 줄**의 지목까지 면제한다"이고 §범위 계측이
  "이 구역에서 **줄을 추가하는 방향은 전부 통과**한다"를 문제로 들었다. 그 성질은 아직 남아 있다.
  **이 커밋으로 #293 을 종결하면 이슈 본문보다 좁게 닫는 것이다** — 종결하려면 잔여를 새 이슈로 분리하고
  그 사실을 PR 에 적어야 한다.
- 완화 요인: 그 절의 컨벤션이 "모든 줄이 `- ` 로 시작"이라 관습을 따르면 M10c 처럼 막힌다(실측 EXIT 1).

### [MEDIUM-1] 블록 경계가 마크다운 구조와 어긋나는 3형태 — 표 · 인용문 · 코드펜스

- **증거**: `D1`(표) · `D4`(인용문) · `D5b`(코드펜스) 전부 **EXIT 0**. `BLOCK_BREAK`/`BLOCK_START` 는
  `|`·`>`·` ``` ` 를 경계로 보지 않아 표 전체·인용문·코드블록이 인접 문단과 한 덩어리가 된다.
  표는 **행이 곧 항목**인데 한 행의 `where installed` 가 다른 행의 지목을 면제한다.
- **현재 도달 가능성 0**: 배포 룰 7종 + 앵커에 표(`^|`) 0줄 · 인용문(`^>`) 0줄(전수 grep). 지금은 못 뚫린다.
- **그래도 남기는 이유**: 이 게이트의 목적이 "앞으로 추가될 줄"을 무는 것이고, 이 리포의 룰은 표를 즐겨 쓴다
  (개발 사본 `.claude/rules/` 는 표투성이다). 자산 목록을 표로 옮기는 순간 그 표는 통째로 무면허 구역이 된다.

### [MEDIUM-2] 중첩 리스트를 부모 항목에서 떼어낸다 — 정당한 면제가 새로 차단된다

- **증거**: `D3` — 부모 항목이 지목하고 하위 불릿이 `없으면 …` 으로 대안을 적는 형태가 **PRE 0 → POST 1**.
  `BLOCK_START` 가 `^\s*` 라 들여쓴 불릿도 "새 항목"으로 보아 부모의 전방 확장을 끊는다.
- CommonMark 상 하위 리스트는 **부모 항목의 내용**이다. 반대 방향(D2, 자식이 부모 조건절 차용)을 막는 것은
  의도로 보이지만, 이 비대칭이 의도인지 부작용인지 주석 어디에도 없다.
- **부수 증거**: `BLOCK_START` 에서 `^\s*` 를 지운 변이(T4)가 **생존**한다 — 이 동작은 어떤 테스트도 안 문다.

### [LOW-1] 커밋 메시지의 "121자"가 실측과 다르다

실측 137자(시작→시작) / 122자(ack 끝→매치 시작). 같은 문장이 게이트 주석 `:154` 에도 복제돼 있어 두 곳이 함께 틀린다.
이 리포는 수치 주장에 실측을 요구한다(`clear-korean-communication`).

### [LOW-2] 문단 중간에 `- ` 로 시작하는 줄이 있으면 같은 문단의 ack 가 끊긴다

`D10` — PRE 0 → POST 1. CommonMark 상 실제 리스트 항목이므로 "오인"이라 부르긴 어렵지만,
**한 문단이 언제 잘리는지**가 주석에 없다. 산문에서 `- 5도 내려간다` 같은 줄은 실제로 나올 수 있다.

### [LOW-3] 의도된 좁힘의 부작용 — 도입 문장·직전 문단의 조건절은 더 이상 면제가 아니다

`D16`·`D17` 모두 PRE 0 → POST 1. 앵커가 "hence the condition on every line" 이라 **컨벤션과는 정합**이지만,
룰을 쓰는 사람에게 "조건절은 그 줄(그 항목) 안에" 라는 새 요구가 생겼다. 게이트 실패 메시지에는 그 안내가 없다
(현재 문구: "지목을 빼거나, 부재를 명시하고 대안을 함께 적어라").

## 잘된 점 (구체적 관찰만)

- **단조성으로 설계한 것**이 이 변경의 안전을 실제로 보장한다. 창을 갈아치우지 않고 교집합을 쓴 덕에
  fuzz 3,000 케이스에서 "전에 잡히던 것이 지금 새는" 경우가 **구조적으로 0**이다.
- **착수 전 실측이 사실이었다.** "면제 2건 · 둘 다 같은 줄 · +30/+23자 · playwright-launch 는 미설치 0종" 네 주장이
  전수 대조에서 모두 참이었다. 추정으로 쓴 문장이 아니다.
- **주석이 실패 사례를 남긴다.** `:152-160` 이 "창만으로는 / 블록만으로는" 양쪽 실패를 다 적어 다음 사람이
  둘 중 하나로 되돌리는 것을 막는다.
- 변경이 **테스트 1파일**로 끝났고 `ACK_WINDOW`·`ABSENCE_ACK` 를 건드리지 않았다(ADR-055 어휘 보존).

## 미검증

- **설치본 경로 미검증** — 이 게이트는 리포의 `templates/` 만 읽는다. 설치된 프로젝트의 `.claude/rules/`·
  `.uzys-agent-harness/` 에서의 거동은 대상 밖이라 돌리지 않았다(Docker 시나리오 미실행).
- **개발 사본 `.claude/rules/`(표 다수)** 는 이 게이트의 검사 대상이 아니다 — MEDIUM-1 의 현실성 근거로만 인용했고
  그 파일들에 대해 게이트를 돌린 것은 아니다.
- **`install-matrix.yml`·릴리즈 CI 미실행** — 태그·배포 단계가 아니라 해당 없음.
- **`npx ecc-agentshield scan` · `npm audit` 미실행** — 테스트 1파일 변경이라 의존성 축 변화 없음. 다만 "돌리지 않았다"고 적는다.
- **성능 축 미측정** — `ackScope` 가 매치마다 전문을 `split("\n")` 하고 `lineAt` 이 선형 스캔이다(현재 매치 5건·문서 최대 157줄이라
  실측 필요를 못 느꼈다). 상주 문서가 수백 줄로 늘면 재고 대상.
- **표·인용문·코드펜스 누출의 "실제 도달"** 은 오늘 기준 0줄이라 합성 파일로만 보였다.

## 처리 결과 (오케스트레이터 · 검증 레인 판정 이후 추가)

위 §판정~§미검증은 검증 레인의 산출물이며 **수정하지 않았다.** 아래는 그 지적에 대한 처분이다.

| 결함 | 처분 | 근거 |
|---|---|---|
| **HIGH-1** 회귀 테스트가 판정 경로를 안 잠근다(T0) | **수정** — `findViolations` 에 테스트 전용 `inject` 훅을 두고 #293 재현을 **게이트 전체 경로**에 태우는 테스트 추가 | 성문 기준 정면 위반이라 선택지가 없었다. 음성 대조로 확인: 호출부를 옛 창으로 되돌리면 `expected [] to include 'implementer'` 로 문다(이전에는 vitest·biome·tsc 전부 초록) |
| **LOW-1** "121자"가 실측과 다름 | **수정** — 주석을 실측 **122자**(조건절 끝 → 새 지목)로 정정하고 기준을 함께 적었다 | 이 리포는 수치 주장에 실측을 요구한다. 121 은 #290 보고서의 근사치를 그대로 옮긴 것이었다 |
| **HIGH-2** 형태를 바꾼 누출 5건(lazy continuation · 한 항목 안 두 자산 등) | **이 PR 밖 — 새 이슈로 분리, #293 은 "이웃 항목 차용"까지만 닫는다** | 검증 레인 권고대로 이슈보다 좁게 닫는다는 사실을 PR 과 이슈 종결에 명시한다. `M16`(한 항목 안 두 자산)은 블록을 더 좁혀도 못 잡는다 — ack 가 **어느 자산에 대한 것인지** 어휘 매칭이 모르기 때문이고, 게이트가 이미 밝힌 한계와 같은 부류다 |
| **MEDIUM-1** 표·인용문·코드펜스가 블록 경계가 아님 | **이 PR 밖 — 같은 새 이슈** | 현재 도달 0(배포 룰에 표·인용문 0줄). 지적대로 이 리포 룰은 표를 즐겨 쓰므로 잠재는 실재한다 |
| **MEDIUM-2 + T4** 중첩 리스트 비대칭 / `^\s*` 변이 생존 | **이 PR 밖 — 같은 새 이슈** | 이 PR 이 만든 과잉 차단이나 현재 도달 0. 비대칭이 의도인지 부작용인지 결정이 필요해 별건으로 다룬다 |
| LOW-2·LOW-3 | **수정 없음** | 의도된 좁힘의 결과이고 앵커 컨벤션("hence the condition on every line")과 정합. 기록으로 남긴다 |

수정 후 재확인(오케스트레이터 직접 실행): T0 변이 → **EXIT 1** · 무변이 → EXIT 0 (5 tests) ·
`npm run ci` → **exit 0 · 1340 passed · branches 88.8%**.

## 범위 밖 관찰 (한 줄)

- 이슈 #289·#294·#295 는 열어 보지 않았다 — 이번 검증 범위 밖.
- 이 게이트의 지목 모집단이 **5건**이다. `references > 4` canary 가 실측 바로 아래라 정상 동작 중이지만,
  모집단이 이 정도면 "전수 대조 이상 없음"이 주는 확신도 그만큼만이다 — 별건으로 남긴다.
