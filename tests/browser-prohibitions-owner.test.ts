import { existsSync } from "node:fs";
import { join, resolve } from "node:path";
import { describe, expect, it } from "vitest";

/**
 * 브라우저 금지문을 **어디가 소유하는가** — 2026-08-12 에 `playwright-launch` 룰을
 * `ui-visual-review` 스킬로 흡수한 결정(사용자 확정)이 유지되는지.
 *
 * **2026-08-30 재판정(#364)에서 3개를 걷어냈다.** 걷어낸 것은 금지문 4종이 스킬 본문에
 * 실재하는지, 금지 블록 안에 있는지, description 이 "열기 전"에 발화하는 문구를 담는지를
 * 문자열로 단언하던 것들이다.
 *
 * **왜 걷었나 — 이미 채택된 룰이 금지하는 형태였다.** `.claude/rules/change-management.md`
 * §자산은 자기 변경 요청 없이 건드리지 않는다 가 *"문장의 의미를 무는 자동 검사는 만들지
 * 마라(3회 우회 실측)"* 로 못박고 있다. 이 게이트는 그 결정(2026-08-26)보다 먼저 만들어져
 * 적용을 피해 있었을 뿐이다. 문구 검사는 **양쪽으로 다 틀린다**: 같은 뜻으로 다시 쓰면
 * 🔴(정당한 개정을 막는다), 어절을 남긴 채 "단, …인 경우는 예외"를 덧붙이면 🟢(뜻이 반대인데
 * 통과). #345 에서 세 라운드 고쳐 봤고 셋 다 뚫렸다.
 *
 * **그 자리를 무엇이 맡나.** 자산 본문의 뜻은 사람과 에이전트가 **이력을 읽어** 판정한다 —
 * `npm run assets:history` 가 자산마다 마지막 변경 커밋과 **그 커밋이 건드린 자산 수**를 낸다.
 * 이 자산에 대해 실제로 작동하는 것을 재판정 때 확인했다(2026-08-13 `35dfb0b`, 자산수 3).
 *
 * **남은 하나는 뜻을 안 읽는다** — 파일이 있나 없나뿐이라 우회로도 오탐도 없다.
 */

const ROOT = resolve(__dirname, "..");

describe("브라우저 금지문의 소유자", () => {
  it("룰은 사라졌다 — 되살아나면 상주 층이 다시 늘어난다", () => {
    // 흡수 결정의 절반은 "스킬로 옮긴다"이고 나머지 절반은 "룰을 상주에서 뺀다"다. 뒤엣것만
    // 조용히 되돌아오면 같은 내용이 두 층에 상주한다. 이 단언은 파일 존재만 보므로 문구 개정과
    // 무관하다 — 걷어낸 3개와 달리 정당한 개정을 막지 않는다.
    expect(existsSync(join(ROOT, "templates/rules/playwright-launch.md"))).toBe(false);
  });
});
